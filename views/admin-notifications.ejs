<!DOCTYPE html>
<html lang="de" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Benachrichtigungen - Admin Dashboard - Doktorum nerede</title>
    <link href="/css/output.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body class="h-full bg-gray-50 font-sans antialiased">
    <div class="min-h-screen">
        <!-- Header -->
        <nav class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50">
            <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-10 py-3 sm:py-4">
                <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3 sm:gap-0">
                    <h1 class="text-xl sm:text-2xl font-bold text-gray-900">Benachrichtigungen</h1>
                    <div class="flex flex-wrap items-center gap-2 sm:gap-4 sm:space-x-4">
                        <a href="/admin" class="text-blue-600 hover:text-blue-700 font-bold text-sm sm:text-base transition-all duration-200 ease-in-out">Zurück zum Dashboard</a>
                        <a href="/" class="text-blue-600 hover:text-blue-700 font-bold text-sm sm:text-base transition-all duration-200 ease-in-out">Zur Hauptseite</a>
                        <form action="/logout" method="POST" class="inline">
                            <button type="submit" class="bg-red-600 hover:bg-red-700 text-white font-bold px-3 sm:px-4 py-1.5 sm:py-2 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 ease-in-out text-sm sm:text-base">Abmelden</button>
                        </form>
                    </div>
                </div>
            </div>
        </nav>

        <main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-10 py-6 sm:py-8">
            <div class="bg-white shadow-md rounded-2xl p-4 sm:p-6 lg:p-8 hover:shadow-lg transition-all duration-200 ease-in-out">
                <h2 class="text-xl sm:text-2xl font-bold text-gray-900 mb-4 sm:mb-6">Massen-E-Mail an alle registrierten Ärzte senden</h2>
                
                <% if (typeof success !== 'undefined' && success) { %>
                    <div class="mb-4 sm:mb-6 rounded-xl bg-green-50 p-3 sm:p-4 border border-green-200">
                        <div class="flex">
                            <div class="flex-shrink-0">
                                <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                                </svg>
                            </div>
                            <div class="ml-3">
                                <h3 class="text-base font-bold text-green-800"><%= success %></h3>
                            </div>
                        </div>
                    </div>
                <% } %>

                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="mb-4 sm:mb-6 rounded-xl bg-red-50 p-3 sm:p-4 border border-red-200">
                        <div class="flex">
                            <div class="flex-shrink-0">
                                <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                                </svg>
                            </div>
                            <div class="ml-3">
                                <h3 class="text-base font-bold text-red-800"><%= error %></h3>
                            </div>
                        </div>
                    </div>
                <% } %>

                <form id="notificationForm" class="space-y-4 sm:space-y-6">
                    <div>
                        <label for="message" class="block text-base sm:text-lg font-bold text-gray-900 mb-2">
                            Nachricht
                        </label>
                        <textarea 
                            id="message" 
                            name="message" 
                            rows="10" 
                            required
                            placeholder="Geben Sie hier die Nachricht ein, die an alle registrierten Ärzte gesendet werden soll..."
                            class="block w-full border border-gray-300 rounded-lg p-3 sm:p-4 text-base font-medium text-gray-900 placeholder:text-gray-400 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-200 ease-in-out resize-y"
                        ></textarea>
                        <p class="mt-2 text-sm text-gray-600">
                            Diese Nachricht wird an alle <span class="font-bold text-blue-600"><%= totalDoctors %></span> registrierten Ärzte gesendet.
                        </p>
                    </div>

                    <div class="flex flex-col sm:flex-row justify-end gap-3 sm:gap-4 pt-4">
                        <button 
                            type="button" 
                            onclick="clearForm()"
                            class="bg-gray-500 hover:bg-gray-600 text-white font-bold px-4 sm:px-6 py-2 sm:py-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 ease-in-out text-base"
                        >
                            Zurücksetzen
                        </button>
                        <button 
                            type="submit" 
                            id="sendButton"
                            class="bg-blue-600 hover:bg-blue-700 text-white font-bold px-4 sm:px-6 py-2 sm:py-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 ease-in-out text-base"
                        >
                            E-Mail senden
                        </button>
                    </div>
                </form>
            </div>
        </main>
    </div>

    <script>
        document.getElementById('notificationForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const message = document.getElementById('message').value.trim();
            const sendButton = document.getElementById('sendButton');
            const originalButtonText = sendButton.textContent;
            
            if (!message) {
                Swal.fire({
                    icon: 'error',
                    title: 'Fehler',
                    text: 'Bitte geben Sie eine Nachricht ein!'
                });
                return;
            }

            // Confirm before sending
            const confirmResult = await Swal.fire({
                title: 'Sind Sie sicher?',
                text: `Diese E-Mail wird an alle ${document.querySelector('.font-bold.text-blue-600').textContent} registrierten Ärzte gesendet.`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#2563eb',
                cancelButtonColor: '#6b7280',
                confirmButtonText: 'Ja, senden',
                cancelButtonText: 'Abbrechen'
            });

            if (!confirmResult.isConfirmed) {
                return;
            }

            // Disable button and show loading
            sendButton.disabled = true;
            sendButton.textContent = 'Wird gesendet...';

            try {
                const response = await fetch('/admin/notifications/send', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ message: message })
                });

                const result = await response.json();

                if (response.ok) {
                    Swal.fire({
                        icon: 'success',
                        title: 'Erfolgreich gesendet',
                        text: result.message || `E-Mail wurde an ${result.sentCount || 'alle'} Ärzte gesendet.`,
                        confirmButtonText: 'OK'
                    }).then(() => {
                        document.getElementById('notificationForm').reset();
                    });
                } else {
                    throw new Error(result.error || 'Fehler beim Senden der E-Mails');
                }
            } catch (error) {
                Swal.fire({
                    icon: 'error',
                    title: 'Fehler',
                    text: error.message || 'Fehler beim Senden der E-Mails. Bitte versuchen Sie es erneut.'
                });
            } finally {
                // Re-enable button
                sendButton.disabled = false;
                sendButton.textContent = originalButtonText;
            }
        });

        function clearForm() {
            if (confirm('Möchten Sie das Formular wirklich zurücksetzen?')) {
                document.getElementById('notificationForm').reset();
            }
        }
    </script>
</body>
</html>


<!DOCTYPE html>
<html lang="tr" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-50 font-sans antialiased">
    <!-- Header -->
    <header class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50">
        <nav class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-10 py-3 sm:py-4">
            <div class="flex flex-col sm:flex-row justify-between items-center gap-3 sm:gap-0">
                <div class="flex items-center">
                    <a href="/" class="text-xl sm:text-2xl font-bold text-blue-600 hover:text-blue-700 transition-all duration-200 ease-in-out">
                        <%= lang === 'tr' ? 'Doktorum nerede?' : 'Doktorum nerede?' %>
                    </a>
                </div>
                <div class="flex items-center flex-wrap gap-3 sm:gap-4 sm:space-x-4 justify-center sm:justify-end">
                    <div class="flex gap-2 sm:gap-4 items-center">
                        <a href="?lang=de" class="<%= lang === 'de' ? 'text-blue-600 font-bold' : 'text-gray-500' %> hover:text-gray-800 transition-all duration-200 ease-in-out text-base">DE</a>
                        <span class="text-gray-300">|</span>
                        <a href="?lang=tr" class="<%= lang === 'tr' ? 'text-blue-600 font-bold' : 'text-gray-500' %> hover:text-gray-800 transition-all duration-200 ease-in-out text-base">TR</a>
                    </div>
                    <a href="/login" class="text-gray-700 hover:text-gray-800 px-3 sm:px-4 py-1.5 sm:py-2 rounded-lg transition-all duration-200 ease-in-out text-base font-medium"><%= lang === 'tr' ? 'Giriş' : 'Login' %></a>
                    <a href="/register" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 sm:px-6 rounded-2xl shadow-md hover:shadow-lg transition-all duration-200 ease-in-out text-sm sm:text-base">
                        <%= lang === 'tr' ? 'Kayıt Ol' : 'Registrieren' %>
                    </a>
                </div>
            </div>
        </nav>
    </header>

    <!-- Parallax Image Section -->
    <div class="relative h-[300px] sm:h-[400px] md:h-[500px] overflow-hidden">
        <div class="parallax-container w-full h-full bg-center bg-no-repeat bg-cover" style="background-image: url('/images/doctors-parallax.jpg');">
            <div class="absolute inset-0 bg-blue-900/10"></div>
        </div>
    </div>

    <!-- Hero Section -->
    <div class="relative isolate bg-white">
        <div class="mx-auto max-w-7xl px-6 py-20 sm:py-24 lg:px-8">
            <div class="text-center space-y-8">
                <h1 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-5xl lg:text-6xl">
                    <% if (lang === 'tr') { %>
                        Türkçe konuşan doktorları
                        <span class="text-blue-600 relative">
                            kolayca bulun
                            <svg class="absolute -bottom-2 w-full" style="bottom: -30px;" viewBox="0 0 300 20" xmlns="http://www.w3.org/2000/svg">
                                <path d="M1 15C50 -5 200 -5 299 15" stroke="#2563eb" stroke-width="4" fill="none"/>
                            </svg>
                        </span>
                    <% } else { %>
                        Türkischsprachige Ärzte
                        <span class="text-blue-600 relative">
                            einfach finden
                            <svg class="absolute -bottom-2 w-full" style="bottom: -30px;" viewBox="0 0 300 20" xmlns="http://www.w3.org/2000/svg">
                                <path d="M1 15C50 -5 200 -5 299 15" stroke="#2563eb" stroke-width="4" fill="none"/>
                            </svg>
                        </span>
                    <% } %>
                </h1>
                <p class="mt-6 text-lg sm:text-xl font-medium leading-8 text-gray-800 max-w-2xl mx-auto">
                    <%= lang === 'tr' ? 'Viyana ve tüm Avusturya\'daki Türkçe konuşan doktorlarla bağlantı kurun' : 'Verbinden Sie sich mit türkischsprachigen Ärzt:innen in Wien und ganz Österreich' %>
                </p>
            </div>
        </div>
    </div>

    <!-- Search Section -->
    <div id="search" class="bg-gray-50 py-16 sm:py-20">
        <div class="mx-auto max-w-7xl px-6 lg:px-8">
            <div class="mx-auto max-w-2xl lg:text-center space-y-6 pt-12">
                <h2 class="text-2xl sm:text-3xl lg:text-4xl font-bold tracking-tight text-gray-900 text-center">
                    <%= lang === 'tr' ? 'Size uygun doktoru bulun' : 'Finden Sie die passende ärztliche Betreuung' %>
                </h2>
                <p class="text-lg sm:text-xl font-medium leading-8 text-gray-800 text-center">
                    <%= lang === 'tr' ? 'Uzmanlık alanı, isim veya konum ile arama yapın' : 'Suchen Sie nach Fachgebiet, Name oder Standort' %>
                </p>
            </div>

            <!-- Search Form -->
            <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8 sm:py-12">
                <div class="bg-white shadow-md rounded-2xl p-4 sm:p-6 lg:p-8 hover:shadow-lg transition-all duration-200 ease-in-out">
                    <form action="/" method="GET" class="space-y-4 sm:space-y-6">
                        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6">
                            <div class="space-y-2">
                                <label for="name" class="block text-base font-bold text-gray-900"><%= t('search.name') %></label>
                                <input type="text" id="name" name="name" 
                                       class="block w-full border border-gray-300 rounded-lg p-3 text-base font-medium text-gray-900 placeholder:text-gray-400 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-200 ease-in-out">
                            </div>
                            <div class="space-y-2">
                                <label for="specialty" class="block text-base font-bold text-gray-900"><%= t('search.specialty') %></label>
                                <select id="specialty" name="specialty" 
                                        class="block w-full border border-gray-300 rounded-lg p-3 text-base font-medium text-gray-900 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-200 ease-in-out">
                                    <option value=""><%= t('search.all') %></option>
                                    <% const specialtyGender = lang === 'tr' ? 'male' : 'male'; %>
                                    <% Object.entries(t('specialties.' + specialtyGender)).forEach(([key, value]) => { %>
                                        <option value="<%= key %>"><%= value %></option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="space-y-2">
                                <label for="zipCode" class="block text-base font-bold text-gray-900"><%= t('search.zipCode') %></label>
                                <select id="zipCode" name="zipCode" 
                                        class="block w-full border border-gray-300 rounded-lg p-3 text-base font-medium text-gray-900 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-200 ease-in-out">
                                    <option value=""><%= t('search.all') %></option>
                                    <% zipCodes.forEach(zipCode => { %>
                                        <option value="<%= zipCode %>"><%= zipCode %></option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="space-y-2">
                                <label for="city" class="block text-base font-bold text-gray-900"><%= t('search.city') %></label>
                                <select id="city" name="city" 
                                        class="block w-full border border-gray-300 rounded-lg p-3 text-base font-medium text-gray-900 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-200 ease-in-out">
                                    <option value=""><%= t('search.all') %></option>
                                    <% cities.forEach(city => { %>
                                        <option value="<%= city %>"><%= city %></option>
                                    <% }); %>
                                </select>
                            </div>
                        </div>
                        <div class="flex justify-end">
                            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-2xl shadow-md hover:shadow-lg transition-all duration-200 ease-in-out text-base">
                                <%= t('search.button') %>
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Results List -->
                <div class="mt-8 sm:mt-12">
                    <div class="bg-white shadow-md rounded-2xl overflow-hidden">
                        <div class="overflow-x-auto">
                            <table class="min-w-full divide-y divide-gray-200">
                                <thead class="bg-gray-50 sticky top-0 z-10">
                                    <tr>
                                        <th scope="col" class="px-2 sm:px-4 py-4 text-left text-base font-bold text-gray-900"><%= t('table.name') %></th>
                                        <th scope="col" class="px-2 sm:px-4 py-4 text-left text-base font-bold text-gray-900"><%= t('table.specialty') %></th>
                                        <th scope="col" class="px-4 sm:px-6 py-4 text-left text-base font-bold text-gray-900"><%= t('table.details') %></th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y divide-gray-200 bg-white">
                                    <% if (doctors && doctors.length > 0) { %>
                                        <% doctors.forEach(doctor => { %>
                                            <tr class="hover:bg-gray-50 transition-all duration-200 ease-in-out">
                                                <td class="px-2 sm:px-4 py-4 text-left text-base font-medium text-gray-800">
                                                    <%= doctor.academicTitle ? doctor.academicTitle + ' ' : '' %>
                                                    <%= doctor.firstName || '' %> <%= doctor.lastName || '' %>
                                                </td>
                                                <td class="px-2 sm:px-4 py-4 text-left text-base font-medium text-gray-800">
                                                    <% 
                                                        const specialtyText = getTranslatedSpecialty(doctor, lang);
                                                    %>
                                                    <%= specialtyText || t('table.notSpecified') %>
                                                </td>
                                                <td class="px-4 sm:px-6 py-4 text-left">
                                                    <a href="/doctor/<%= formatNameForUrl(doctor.firstName || '', doctor.lastName || '') %>" 
                                                       class="text-blue-600 hover:text-blue-700 font-bold transition-all duration-200 ease-in-out text-base">
                                                        <%= t('search.toProfile') %> →
                                                    </a>
                                                </td>
                                            </tr>
                                        <% }); %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="3" class="px-4 sm:px-6 py-8 text-center text-base font-medium text-gray-600">
                                                <%= t('search.noResults') %>
                                            </td>
                                        </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                        
                        <!-- Pagination -->
                        <% if (doctors && doctors.length > 0) { %>
                            <div class="bg-gray-50 px-4 py-4 flex flex-col sm:flex-row items-center justify-between border-t border-gray-200 sm:px-6 gap-4">
                                <!-- Mobile: Simple Previous/Next -->
                                <div class="flex-1 flex justify-between md:hidden w-full">
                                    <% 
                                        const prevParams = new URLSearchParams(queryParams || '');
                                        prevParams.set('page', currentPage - 1);
                                        const nextParams = new URLSearchParams(queryParams || '');
                                        nextParams.set('page', currentPage + 1);
                                    %>
                                    <% if (currentPage > 1) { %>
                                        <a href="?<%= prevParams.toString() %>#search" 
                                           onclick="event.preventDefault(); window.location.href = '?<%= prevParams.toString() %>#search'; const searchEl = document.getElementById('search'); if (searchEl) { setTimeout(() => searchEl.scrollIntoView({ behavior: 'smooth', block: 'start' }), 100); }"
                                           class="relative inline-flex items-center px-4 py-2 border-2 border-blue-600 text-base font-bold rounded-lg text-blue-600 bg-white hover:bg-blue-50 transition-colors">
                                            <svg class="h-5 w-5 mr-2" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                                                <path fill-rule="evenodd" d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z" clip-rule="evenodd" />
                                            </svg>
                                            <%= lang === 'tr' ? 'Önceki' : 'Zurück' %>
                                        </a>
                                    <% } else { %>
                                        <span class="relative inline-flex items-center px-4 py-2 border-2 border-gray-300 text-base font-medium rounded-lg text-gray-400 bg-white cursor-not-allowed">
                                            <svg class="h-5 w-5 mr-2" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                                                <path fill-rule="evenodd" d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z" clip-rule="evenodd" />
                                            </svg>
                                            <%= lang === 'tr' ? 'Önceki' : 'Zurück' %>
                                        </span>
                                    <% } %>
                                    <% if (currentPage < totalPages) { %>
                                        <a href="?<%= nextParams.toString() %>#search" 
                                           onclick="event.preventDefault(); window.location.href = '?<%= nextParams.toString() %>#search'; const searchEl = document.getElementById('search'); if (searchEl) { setTimeout(() => searchEl.scrollIntoView({ behavior: 'smooth', block: 'start' }), 100); }"
                                           class="relative inline-flex items-center px-4 py-2 border-2 border-blue-600 text-base font-bold rounded-lg text-blue-600 bg-white hover:bg-blue-50 transition-colors">
                                            <%= lang === 'tr' ? 'Sonraki' : 'Weiter' %>
                                            <svg class="h-5 w-5 ml-2" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                                                <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                                            </svg>
                                        </a>
                                    <% } else { %>
                                        <span class="relative inline-flex items-center px-4 py-2 border-2 border-gray-300 text-base font-medium rounded-lg text-gray-400 bg-white cursor-not-allowed">
                                            <%= lang === 'tr' ? 'Sonraki' : 'Weiter' %>
                                            <svg class="h-5 w-5 ml-2" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                                                <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                                            </svg>
                                        </span>
                                    <% } %>
                                </div>
                                <!-- Desktop: Full pagination with arrows -->
                                <div class="hidden md:flex flex-1 items-center justify-between w-full">
                                    <div>
                                        <p class="text-sm text-gray-700">
                                            <%= lang === 'tr' ? 'Gösteriliyor' : 'Zeige' %>
                                            <span class="font-medium"><%= (currentPage - 1) * doctorsPerPage + 1 %></span>
                                            <%= lang === 'tr' ? '-' : ' bis' %>
                                            <span class="font-medium"><%= Math.min(currentPage * doctorsPerPage, totalDoctors) %></span>
                                            <%= lang === 'tr' ? ' toplam' : ' von' %>
                                            <span class="font-medium"><%= totalDoctors %></span>
                                            <%= lang === 'tr' ? ' doktor' : ' Ärzten' %>
                                        </p>
                                    </div>
                                    <div>
                                        <nav class="relative z-0 inline-flex rounded-md shadow-sm -space-x-px" aria-label="Pagination">
                                            <% 
                                                const prevNavParams = new URLSearchParams(queryParams || '');
                                                prevNavParams.set('page', currentPage - 1);
                                                const nextNavParams = new URLSearchParams(queryParams || '');
                                                nextNavParams.set('page', currentPage + 1);
                                            %>
                                            <% if (currentPage > 1) { %>
                                                <a href="?<%= prevNavParams.toString() %>#search" 
                                                   onclick="event.preventDefault(); window.location.href = '?<%= prevNavParams.toString() %>#search'; const searchEl = document.getElementById('search'); if (searchEl) { setTimeout(() => searchEl.scrollIntoView({ behavior: 'smooth', block: 'start' }), 100); }"
                                                   class="relative inline-flex items-center px-3 py-2 rounded-l-md border-2 border-gray-300 bg-white text-base font-bold text-gray-700 hover:bg-gray-50 hover:border-blue-600 hover:text-blue-600 transition-all">
                                                    <span class="sr-only"><%= lang === 'tr' ? 'Önceki' : 'Vorherige' %></span>
                                                    <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                                        <path fill-rule="evenodd" d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z" clip-rule="evenodd" />
                                                    </svg>
                                                </a>
                                            <% } else { %>
                                                <span class="relative inline-flex items-center px-3 py-2 rounded-l-md border-2 border-gray-300 bg-white text-base font-medium text-gray-300 cursor-not-allowed">
                                                    <span class="sr-only"><%= lang === 'tr' ? 'Önceki' : 'Vorherige' %></span>
                                                    <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                                        <path fill-rule="evenodd" d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z" clip-rule="evenodd" />
                                                    </svg>
                                                </span>
                                            <% } %>
                                            
                                            <% 
                                                let startPage = Math.max(1, currentPage - 2);
                                                let endPage = Math.min(totalPages, currentPage + 2);
                                                if (endPage - startPage < 4) {
                                                    if (startPage === 1) {
                                                        endPage = Math.min(5, totalPages);
                                                    } else {
                                                        startPage = Math.max(1, totalPages - 4);
                                                    }
                                                }
                                            %>
                                            <% for (let i = startPage; i <= endPage; i++) { %>
                                                <% 
                                                    const pageParams = new URLSearchParams(queryParams || '');
                                                    pageParams.set('page', i);
                                                %>
                                                <% if (i === currentPage) { %>
                                                    <span aria-current="page" class="relative inline-flex items-center px-4 py-2 border-2 border-blue-600 bg-blue-50 text-base font-bold text-blue-600">
                                                        <%= i %>
                                                    </span>
                                                <% } else { %>
                                                    <a href="?<%= pageParams.toString() %>#search" 
                                                       onclick="event.preventDefault(); window.location.href = '?<%= pageParams.toString() %>#search'; const searchEl = document.getElementById('search'); if (searchEl) { setTimeout(() => searchEl.scrollIntoView({ behavior: 'smooth', block: 'start' }), 100); }"
                                                       class="relative inline-flex items-center px-4 py-2 border-2 border-gray-300 bg-white text-base font-medium text-gray-700 hover:bg-gray-50 hover:border-blue-600 hover:text-blue-600 transition-all">
                                                        <%= i %>
                                                    </a>
                                                <% } %>
                                            <% } %>
                                            
                                            <% if (currentPage < totalPages) { %>
                                                <a href="?<%= nextNavParams.toString() %>#search" 
                                                   onclick="event.preventDefault(); window.location.href = '?<%= nextNavParams.toString() %>#search'; const searchEl = document.getElementById('search'); if (searchEl) { setTimeout(() => searchEl.scrollIntoView({ behavior: 'smooth', block: 'start' }), 100); }"
                                                   class="relative inline-flex items-center px-3 py-2 rounded-r-md border-2 border-gray-300 bg-white text-base font-bold text-gray-700 hover:bg-gray-50 hover:border-blue-600 hover:text-blue-600 transition-all">
                                                    <span class="sr-only"><%= lang === 'tr' ? 'Sonraki' : 'Nächste' %></span>
                                                    <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                                        <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                                                    </svg>
                                                </a>
                                            <% } else { %>
                                                <span class="relative inline-flex items-center px-3 py-2 rounded-r-md border-2 border-gray-300 bg-white text-base font-medium text-gray-300 cursor-not-allowed">
                                                    <span class="sr-only"><%= lang === 'tr' ? 'Sonraki' : 'Nächste' %></span>
                                                    <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                                        <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                                                    </svg>
                                                </span>
                                            <% } %>
                                        </nav>
                                    </div>
                                </div>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Featured Doctor Section -->
    <%- include('partials/featuredDoctor') %>

    <%- include('partials/blogPosts') %>

    <!-- Footer -->
    <footer class="mt-16" style="background-color: #4338ca;">
        <div class="mx-auto max-w-7xl px-6 py-12 md:flex md:items-center md:justify-between lg:px-8">
            <div class="flex justify-center space-x-6 md:order-2">
                <a href="/impressum" class="text-white hover:text-gray-100 transition-all duration-200 ease-in-out">Impressum</a>
                <span class="text-white">|</span>
                <a href="/datenschutz" class="text-white hover:text-gray-100 transition-all duration-200 ease-in-out">Datenschutz</a>
            </div>
            <div class="mt-8 md:order-1 md:mt-0">
                <p class="text-center text-sm leading-5 text-white">
                    &copy; <%= new Date().getFullYear() %> Doktorum nerede. Alle Rechte vorbehalten.
                </p>
            </div>
        </div>
    </footer>

    <script>
        // Handle scroll to search section when hash is present or after navigation
        document.addEventListener('DOMContentLoaded', function() {
            // Check if we have a hash pointing to search
            if (window.location.hash === '#search') {
                const searchEl = document.getElementById('search');
                if (searchEl) {
                    setTimeout(() => {
                        searchEl.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }, 100);
                }
            }
        });
        
        // Also handle hash change events
        window.addEventListener('hashchange', function() {
            if (window.location.hash === '#search') {
                const searchEl = document.getElementById('search');
                if (searchEl) {
                    setTimeout(() => {
                        searchEl.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }, 100);
                }
            }
        });
    </script>
</body>
</html>
